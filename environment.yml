name: ring-dataset-gen
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - numpy>=1.20.0
  - pyyaml>=6.0
  - pydantic>=2.0.0
  - tqdm>=4.60.0
  - pip
  - pip:
    # Optional: opencv-python for QA checks (uncomment if needed)
    # - opencv-python>=4.5.0

